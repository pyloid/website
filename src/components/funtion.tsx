'use client';

const Function = ({ text }: { text: string }) => (
  <span className='font-mono bg-primary/10 px-2 py-1 rounded text-sm text-primary'>
    {text}
  </span>
);

export default Function;
